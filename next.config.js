/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['fluffy.umkmbedigital.com', 'res.cloudinary.com'],
  },
}

module.exports = nextConfig
